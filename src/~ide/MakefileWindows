.PHONY: all clean clock options

TARGET = 

all: clock options

clean: TARGET = clean
clean: all

clock:
	mingw32-make -f gcc-clock.make $(TARGET)

options:
	mingw32-make -f gcc-options.make $(TARGET)
