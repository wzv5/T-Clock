.PHONY: all clean clock options

TARGET = 

all: clock options

clean: TARGET = clean
clean: all

clock:
	cd .. && mingw32-make -f Makefile $(TARGET)

options:
	mingw32-make -f gcc-options.make $(TARGET)
